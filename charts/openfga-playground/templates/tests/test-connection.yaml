apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openfga-playground.fullname" . }}-test-connection"
  labels:
    {{- include "openfga-playground.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openfga-playground.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
