apiVersion: v1
kind: Service
metadata:
  name: {{ include "openfga-playground.fullname" . }}
  labels:
    {{- include "openfga-playground.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openfga-playground.selectorLabels" . | nindent 4 }}
